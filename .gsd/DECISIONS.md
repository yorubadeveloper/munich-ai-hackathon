# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | 2026-06-14 | M002-qqpa83 | What charting library to use for the visual comparison chart in the Next.js dossier UI. | Recharts | Recharts provides simple, declarative React components that look great out of the box and natively support radar/bar charts perfect for comparing multiple labels. | Yes | collaborative |
| D002 | 2026-06-14 | M002-qqpa83 | How to handle fine-tuning failures if the Pioneer training API is inaccessible. | Graceful Degradation | Prevents a fragile external dependency (training API during a hackathon) from blocking the core evaluation pipeline and evidence display. The pipeline will log the error and continue using the base model's evaluation results. | Yes | collaborative |
| D003 |  | M002-qqpa83 | What charting library to use for the visual comparison chart in the Next.js dossier UI. | Recharts | Recharts provides simple, declarative React components that look great out of the box and natively support radar/bar charts perfect for comparing multiple labels. | Yes | collaborative |
| D004 |  | M002-qqpa83 | How to handle fine-tuning failures if the Pioneer training API is inaccessible. | Graceful Degradation | Prevents a fragile external dependency (training API during a hackathon) from blocking the core evaluation pipeline and evidence display. The pipeline will log the error and continue using the base model's evaluation results. | Yes | collaborative |
| D005 | M001-8itnlq/S04 | M001-8itnlq/S04 | How to integrate fal.ai visual generation into the company research pipeline | Fire-and-forget after research.run() with circuit-breaking (empty key check, try/except, 30s timeout), storing results as EvidenceEvents | fal must be strictly non-blocking per the hackathon constraint. Injecting after research completes means the pipeline never waits on fal, and storing results as EvidenceEvents keeps it consistent with the S01/S02 evidence trail contract. Missing credentials produce error events rather than crashes. | Yes | agent |
| D006 | M002-qqpa83 context discussion | M002-qqpa83 | How to persist generated evaluation datasets, result summaries, and conditional training batches for Pioneer evaluation auditability. | Version key artifacts locally in predictable backend eval folders while keeping secrets and raw API logs out of stored artifacts. | The milestone is judge-facing proof for Fastino. Versioned local artifacts make synthetic labels, metrics, and optional training inputs inspectable and reusable for demos, while PostgreSQL remains the source surfaced in the product dossier. | Yes | collaborative |
| D007 | M002-qqpa83 context discussion | M002-qqpa83 | What operational status detail the pioneer-eval evidence event should expose. | Persist full lifecycle status covering dataset generation, Pioneer inference, Gemini extraction, fine-tuning, persistence, chart readiness, metrics, and degraded reasons. | Full lifecycle status gives judges and downstream UI code an auditable explanation for partial failures instead of only showing final scores or silently hiding skipped fine-tuning. | Yes | collaborative |
| D008 | M002-qqpa83 | architecture | Where to place the evaluation pipeline code | New backend/eval/ module with generator.py, evaluator.py, metrics.py, and persist.py | Separates evaluation concerns from the main orchestrator. Scripts are testable with pytest, integrate with the existing backend structure, and can write outputs cleanly. Matches the research recommendation. | Yes | agent |
| D009 | M002-qqpa83 | architecture | Slice decomposition strategy for M002 | Three slices: S01 (eval engine + metrics + pytest), S02 (Pioneer training + conditional fine-tuning), S03 (DB persistence + Recharts chart) | S01 retires metric calculation unknowns with pure-backend testable code. S02 handles the highest-risk external API integration (Pioneer training) with graceful fallback. S03 delivers the user-visible chart — low risk since it reuses M001 patterns (EvidenceEvent + Recharts). | Yes | agent |
