# BLOCKER — auto-mode recovery failed

Unit `reactive-execute` for `M001-8itnlq/S05/reactive+T01,T02` failed to produce this artifact after idle recovery exhausted all retries.

**Reason**: hard recovery exhausted 1 attempts without producing the artifact.

This placeholder was written by auto-mode so the pipeline can advance.
Review and replace this file before relying on downstream artifacts.