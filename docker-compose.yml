services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: huntagent
      POSTGRES_PASSWORD: huntagent
      POSTGRES_DB: huntagent
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U huntagent"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend:/app
      - /app/.venv
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    env_file: .env
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    volumes:
      - ./frontend:/app
      - frontend_node_modules:/app/node_modules
      - /app/.next
    # Reconcile deps with package.json on every start, then run dev.
    # This keeps the container in sync even if package.json changed.
    command: sh -c "npm install && npm run dev"

volumes:
  pgdata:
  frontend_node_modules:
