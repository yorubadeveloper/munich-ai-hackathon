# Dependencies are managed with uv via pyproject.toml + uv.lock.
# This file is a plain-pip mirror kept in sync for convenience only.
# Canonical install:  uv sync   (or)   uv pip install -r pyproject.toml
#
# Targets Python 3.13 — a few pins were bumped from the original spec
# because asyncpg<0.30 and sqlalchemy<2.0.31 do not build/run on 3.13.
fastapi==0.111.0
uvicorn[standard]==0.29.0
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic==1.13.1
pydantic-settings==2.2.1
httpx==0.27.0
python-telegram-bot==21.3
google-generativeai==0.7.0
tavily-python==0.3.3
python-dotenv==1.0.1
apscheduler==3.10.4
